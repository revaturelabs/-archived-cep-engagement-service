#Zuul's Config

server:
  port: 9015

spring:
  application:
    name: zuul

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://${EUREKA_HOSTED_IP:127.0.0.1}:9999/eureka

ribbon:
  ReadTimeout: 60000


zuul:
  sensitiveHeaders: Cookie, Set-Cookie #Zuul will block these and only these headers from continuing to other services.
  routes:
    register:
      path: /api/getUsers
      service-id: register-user
      strip-prefix: true
    newUser:
      path: /users/add
      service-id: cep-engagement-service
      strip-prefix: true
    login:
      path: /authenticate
      service-id: cep-engagement-service
      strip-prefix: true
    test1:
      path: /UB/batchesbyuser
      service-id: cep-engagement-service
      strip-prefix: true
    catch-all:
      path: /**
      service-id: cep-engagement-service
      strip-prefix: true

